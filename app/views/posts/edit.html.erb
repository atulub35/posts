<%= turbo_frame_tag "edit_post" do %>
    <%= render :partial => "form", :locals => { path: post_path(@post), method: :put, title: @post&.id.present? ? params[:type] == "edit" ? "Update Post" : "Delete Post" : "Create Post" } %>
<% end %>

<% unless @post.errors.any? %>
    <%= turbo_stream.prepend "posts" do %>
        <%= render partial: "posts/post", locals: { post: @post, editable: current_user == @post.user } %>
    <% end %>
<% end %>

<%= turbo_stream.append "flash-notification", partial: "shared/flash/notification" %>
<turbo-stream action="append" target="chat-messages">
    <template>
        <div class="my-4 <%= @message.role == 'user' ? 'user-message p-3 bg-secondary text-white rounded' : 'ai-message' %>">
            <%= @message.content %>
        </div>
    </template>
</turbo-stream>
